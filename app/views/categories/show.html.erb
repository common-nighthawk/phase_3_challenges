<h2><%= @category.name %></h2>

<h2>Listings</h2>
<%= render @category.listings %>

<%= link_to 'Edit this category', edit_category_path(@category) %></br>
<%= link_to 'Destroy', category_path(@category), method: :delete, data: { confirm: 'Are you sure?' } %></br>
<%= link_to 'View all categories', categories_path %></br>
